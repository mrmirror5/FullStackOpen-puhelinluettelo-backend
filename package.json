{
  "name": "puhelinluettelo_backend",
  "version": "1.0.0",
  "description": "Puhelinluettelosovelluksen backend",
  "license": "ISC",
  "author": "Miro Armah",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../puhelin_luettelo/puhelinLuettelo && npm run build && cp -r dist ../../backend",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "morgan": "^1.11.0"
  }
}
